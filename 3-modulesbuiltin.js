// Modules- encapsulated code (only share the minimum)
const names = require("./4-names");
const sayHi = require("./5-utils");
const sums = require("./mind-grenade");
// console.log(names);

sums();
sayHi(names.john);
sayHi(names.gelila);
sayHi(names.matania);
